export async function last(source, predicate = async () => true) {
    let i = 0, result;
    for await (let item of source) {
        if (await predicate(item, i++)) {
            result = item;
        }
    }
    return result;
}

//# sourceMappingURL=last.mjs.map
