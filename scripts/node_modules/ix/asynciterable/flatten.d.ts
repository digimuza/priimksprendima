import { AsyncIterableX } from './asynciterablex';
export declare class FlattenAsyncIterable<TSource> extends AsyncIterableX<TSource> {
    private _source;
    private _depth;
    constructor(source: AsyncIterable<TSource>, depth: number);
    private _flatten;
    [Symbol.asyncIterator](): AsyncIterator<TSource>;
}
export declare function flatten<T>(source: AsyncIterable<T>, depth?: number): AsyncIterableX<T>;
