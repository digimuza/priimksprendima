import { repeatStatic } from '../iterable/repeat';
import { _catchAll } from './catch';
export function retry(source, count = -1) {
    return _catchAll(repeatStatic(source, count));
}

//# sourceMappingURL=retry.mjs.map
