export async function count(source, fn = async () => true) {
    let i = 0;
    for await (let item of source) {
        if (await fn(item)) {
            i++;
        }
    }
    return i;
}

//# sourceMappingURL=count.mjs.map
