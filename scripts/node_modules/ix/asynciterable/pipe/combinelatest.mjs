import { CombineLatestAsyncIterable } from '../combinelatest';
import { identityAsync } from '../../internal/identity';
/* tslint:enable:max-line-length */
export function combineLatest(...sources) {
    return function combineLatestOperatorFunction(source) {
        let fn = (sources.shift() || identityAsync);
        if (fn && typeof fn !== 'function') {
            sources.unshift(fn);
            fn = identityAsync;
        }
        return new CombineLatestAsyncIterable([source, ...sources], fn);
    };
}

//# sourceMappingURL=combinelatest.mjs.map
