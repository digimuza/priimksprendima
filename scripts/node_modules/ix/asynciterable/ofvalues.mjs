import { AsyncIterableX } from './asynciterablex';
class OfValuesAsyncIterable extends AsyncIterableX {
    constructor(source) {
        super();
        this._source = source;
    }
    [Symbol.asyncIterator]() {
        return AsyncIterableX.from(Object.keys(this._source), key => this._source[key])[Symbol.asyncIterator]();
    }
}
export function ofValues(source) {
    return new OfValuesAsyncIterable(source);
}

//# sourceMappingURL=ofvalues.mjs.map
