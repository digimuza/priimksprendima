import { AsyncIterableX } from './asynciterablex';
function makeTuple(x, y) {
    return [x, y];
}
class OfEntriesAsyncIterable extends AsyncIterableX {
    constructor(source) {
        super();
        this._source = source;
    }
    [Symbol.asyncIterator]() {
        return AsyncIterableX.from(Object.keys(this._source), key => makeTuple(key, this._source[key]))[Symbol.asyncIterator]();
    }
}
export function ofEntries(source) {
    return new OfEntriesAsyncIterable(source);
}

//# sourceMappingURL=ofentries.mjs.map
