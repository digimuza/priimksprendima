import { comparer } from '../internal/comparer';
export async function includes(source, searchElement, fromIndex = 0) {
    let i = 0;
    if (Math.abs(fromIndex)) {
        fromIndex = 0;
    }
    for await (let item of source) {
        if (i++ > fromIndex && comparer(item, searchElement)) {
            return true;
        }
    }
    return false;
}

//# sourceMappingURL=includes.mjs.map
