import { identityAsync } from '../internal/identity';
export async function sum(source, selector = identityAsync) {
    let sum = 0;
    for await (let item of source) {
        sum += await selector(item);
    }
    return sum;
}

//# sourceMappingURL=sum.mjs.map
