export async function some(source, predicate) {
    let i = 0;
    for await (let item of source) {
        if (await predicate(item, i++)) {
            return true;
        }
    }
    return false;
}

//# sourceMappingURL=some.mjs.map
