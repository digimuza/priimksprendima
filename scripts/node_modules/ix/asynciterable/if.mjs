import { defer } from './defer';
import { empty } from './empty';
export function _if(fn, thenSource, elseSource = empty()) {
    return defer(async () => ((await fn()) ? thenSource : elseSource));
}

//# sourceMappingURL=if.mjs.map
