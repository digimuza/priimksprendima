export async function elementAt(source, index) {
    for await (let item of source) {
        if (index === 0) {
            return item;
        }
        index--;
    }
    return undefined;
}

//# sourceMappingURL=elementat.mjs.map
