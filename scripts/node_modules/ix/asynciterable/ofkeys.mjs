import { AsyncIterableX } from './asynciterablex';
class OfKeysAsyncIterable extends AsyncIterableX {
    constructor(source) {
        super();
        this._source = source;
    }
    [Symbol.asyncIterator]() {
        return AsyncIterableX.from(Object.keys(this._source))[Symbol.asyncIterator]();
    }
}
export function ofKeys(source) {
    return new OfKeysAsyncIterable(source);
}

//# sourceMappingURL=ofkeys.mjs.map
