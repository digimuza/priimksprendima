export async function every(source, predicate) {
    let i = 0;
    for await (let item of source) {
        if (!await predicate(item, i++)) {
            return false;
        }
    }
    return true;
}

//# sourceMappingURL=every.mjs.map
