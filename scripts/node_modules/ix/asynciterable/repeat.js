"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const asynciterablex_1 = require("./asynciterablex");
class RepeatAsyncIterable extends asynciterablex_1.AsyncIterableX {
    constructor(source, count) {
        super();
        this._source = source;
        this._count = count;
    }
    async *[Symbol.asyncIterator]() {
        if (this._count === -1) {
            while (1) {
                for await (let item of this._source) {
                    yield item;
                }
            }
        }
        else {
            for (let i = 0; i < this._count; i++) {
                for await (let item of this._source) {
                    yield item;
                }
            }
        }
    }
}
exports.RepeatAsyncIterable = RepeatAsyncIterable;
function repeat(source, count = -1) {
    return new RepeatAsyncIterable(source, count);
}
exports.repeat = repeat;
function repeatStatic(value, count = -1) {
    return new RepeatAsyncIterable(asynciterablex_1.AsyncIterableX.of(value), count);
}
exports.repeatStatic = repeatStatic;

//# sourceMappingURL=repeat.js.map
