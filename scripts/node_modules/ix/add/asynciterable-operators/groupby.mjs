import { AsyncIterableX } from '../../asynciterable/asynciterablex';
import { groupBy, groupByResultIdentityAsync } from '../../asynciterable/groupby';
import { identityAsync } from '../../internal/identity';
/**
 * @ignore
 */
export function groupByProto(keySelector, elementSelector = identityAsync, resultSelector = groupByResultIdentityAsync) {
    return groupBy(this, keySelector, elementSelector, resultSelector);
}
AsyncIterableX.prototype.groupBy = groupByProto;

//# sourceMappingURL=groupby.mjs.map
