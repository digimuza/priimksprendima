import { IterableX } from '../../iterable/iterablex';
import { max } from '../../iterable/max';
/**
 * @ignore
 */
export function maxProto(fn = x => x) {
    return max(this, fn);
}
IterableX.prototype.max = maxProto;

//# sourceMappingURL=max.mjs.map
