import { IterableX } from '../../iterable/iterablex';
import { last } from '../../iterable/last';
export function lastProto(predicate = () => true) {
    return last(this, predicate);
}
IterableX.prototype.last = lastProto;

//# sourceMappingURL=last.mjs.map
