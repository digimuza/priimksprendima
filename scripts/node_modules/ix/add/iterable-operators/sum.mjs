import { IterableX } from '../../iterable/iterablex';
import { sum } from '../../iterable/sum';
/**
 * @ignore
 */
export function sumProto(fn = x => x) {
    return sum(this, fn);
}
IterableX.prototype.sum = sumProto;

//# sourceMappingURL=sum.mjs.map
