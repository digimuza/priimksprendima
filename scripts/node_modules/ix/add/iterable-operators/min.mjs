import { IterableX } from '../../iterable/iterablex';
import { min } from '../../iterable/min';
/**
 * @ignore
 */
export function minProto(fn = x => x) {
    return min(this, fn);
}
IterableX.prototype.min = minProto;

//# sourceMappingURL=min.mjs.map
