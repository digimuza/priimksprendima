import { IterableX } from '../../iterable/iterablex';
import { groupBy, groupByResultIdentity } from '../../iterable/groupby';
import { identity } from '../../internal/identity';
/**
 * @ignore
 */
export function groupByProto(keySelector, elementSelector = identity, resultSelector = groupByResultIdentity) {
    return groupBy(this, keySelector, elementSelector, resultSelector);
}
IterableX.prototype.groupBy = groupByProto;

//# sourceMappingURL=groupby.mjs.map
