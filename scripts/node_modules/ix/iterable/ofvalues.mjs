import { IterableX } from './iterablex';
import { map } from './map';
class OfValuesIterable extends IterableX {
    constructor(source) {
        super();
        this._source = source;
    }
    [Symbol.iterator]() {
        return map(Object.keys(this._source), key => this._source[key])[Symbol.iterator]();
    }
}
export function ofValues(source) {
    return new OfValuesIterable(source);
}

//# sourceMappingURL=ofvalues.mjs.map
