import { IterableX } from './iterablex';
import { map } from './map';
function makeTuple(x, y) {
    return [x, y];
}
class OfEntriesIterable extends IterableX {
    constructor(source) {
        super();
        this._source = source;
    }
    [Symbol.iterator]() {
        return map(Object.keys(this._source), key => makeTuple(key, this._source[key]))[Symbol.iterator]();
    }
}
export function ofEntries(source) {
    return new OfEntriesIterable(source);
}

//# sourceMappingURL=ofentries.mjs.map
