"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const iterablex_1 = require("./iterablex");
class RepeatIterable extends iterablex_1.IterableX {
    constructor(source, count) {
        super();
        this._source = source;
        this._count = count;
    }
    *[Symbol.iterator]() {
        if (this._count === -1) {
            while (1) {
                for (let item of this._source) {
                    yield item;
                }
            }
        }
        else {
            for (let i = 0; i < this._count; i++) {
                for (let item of this._source) {
                    yield item;
                }
            }
        }
    }
}
exports.RepeatIterable = RepeatIterable;
function repeat(source, count = -1) {
    return new RepeatIterable(source, count);
}
exports.repeat = repeat;
function repeatStatic(value, count = -1) {
    return new RepeatIterable(iterablex_1.IterableX.of(value), count);
}
exports.repeatStatic = repeatStatic;

//# sourceMappingURL=repeat.js.map
