export function first(source, predicate = () => true) {
    let i = 0;
    for (let item of source) {
        if (predicate(item, i++)) {
            return item;
        }
    }
    return undefined;
}

//# sourceMappingURL=first.mjs.map
