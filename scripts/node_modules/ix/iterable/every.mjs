export function every(source, predicate) {
    let i = 0;
    for (let item of source) {
        if (!predicate(item, i++)) {
            return false;
        }
    }
    return true;
}

//# sourceMappingURL=every.mjs.map
