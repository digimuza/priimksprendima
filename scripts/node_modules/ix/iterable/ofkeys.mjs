import { IterableX } from './iterablex';
class OfKeysIterable extends IterableX {
    constructor(source) {
        super();
        this._source = source;
    }
    [Symbol.iterator]() {
        return Object.keys(this._source)[Symbol.iterator]();
    }
}
export function ofKeys(source) {
    return new OfKeysIterable(source);
}

//# sourceMappingURL=ofkeys.mjs.map
