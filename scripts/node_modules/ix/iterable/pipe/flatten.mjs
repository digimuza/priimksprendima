import { FlattenIterable } from '../flatten';
export function flatten(depth = Infinity) {
    return function flattenOperatorFunction(source) {
        return new FlattenIterable(source, depth);
    };
}

//# sourceMappingURL=flatten.mjs.map
