import { retry as retryStatic } from '../retry';
export function retry(count = -1) {
    return function retryOperatorFunction(source) {
        return retryStatic(source, count);
    };
}

//# sourceMappingURL=retry.mjs.map
