export function last(source, predicate = () => true) {
    let i = 0, result;
    for (let item of source) {
        if (predicate(item, i++)) {
            result = item;
        }
    }
    return result;
}

//# sourceMappingURL=last.mjs.map
