import { identity } from '../internal/identity';
export function sum(source, fn = identity) {
    let sum = 0;
    for (let item of source) {
        sum += fn(item);
    }
    return sum;
}

//# sourceMappingURL=sum.mjs.map
