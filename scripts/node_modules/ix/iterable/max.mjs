import { identity } from '../internal/identity';
export function max(source, fn = identity) {
    let atleastOnce = false;
    let value = -Infinity;
    for (let item of source) {
        if (!atleastOnce) {
            atleastOnce = true;
        }
        let x = fn(item);
        if (x > value) {
            value = x;
        }
    }
    if (!atleastOnce) {
        throw new Error('Sequence contains no elements');
    }
    return value;
}

//# sourceMappingURL=max.mjs.map
