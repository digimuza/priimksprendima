export function some(source, comparer) {
    let i = 0;
    for (let item of source) {
        if (comparer(item, i++)) {
            return true;
        }
    }
    return false;
}

//# sourceMappingURL=some.mjs.map
