import { map } from './map';
function plucker(props, length) {
    const mapper = (x) => {
        let currentProp = x;
        for (let i = 0; i < length; i++) {
            const p = currentProp[props[i]];
            if (typeof p !== 'undefined') {
                currentProp = p;
            }
            else {
                return undefined;
            }
        }
        return currentProp;
    };
    return mapper;
}
export function pluck(source, ...args) {
    return map(source, plucker(args, args.length));
}

//# sourceMappingURL=pluck.mjs.map
