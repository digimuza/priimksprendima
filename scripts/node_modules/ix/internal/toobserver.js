"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const isiterable_1 = require("./isiterable");
const noop = (_) => {
    /**/
};
function toObserver(next, error, complete) {
    const observer = next;
    if (observer && typeof observer === 'object') {
        return {
            next: isiterable_1.isFunction(observer.next) ? x => observer.next(x) : noop,
            error: isiterable_1.isFunction(observer.error) ? e => observer.error(e) : noop,
            complete: isiterable_1.isFunction(observer.complete) ? () => observer.complete() : noop
        };
    }
    return {
        next: isiterable_1.isFunction(next) ? next : noop,
        error: isiterable_1.isFunction(error) ? error : noop,
        complete: isiterable_1.isFunction(complete) ? complete : noop
    };
}
exports.toObserver = toObserver;

//# sourceMappingURL=toobserver.js.map
