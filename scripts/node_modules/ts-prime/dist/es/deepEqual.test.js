import { deepEqual } from "./deepEqual";
describe('deepEqual tests', function () {
    test("Simple equality", function () {
        var obj1 = {
            a: 1
        };
        expect(deepEqual(obj1, { a: 1 })).toEqual(true);
    });
});
